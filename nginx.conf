server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA: redirecionar todas as rotas para index.html
    location / {
        try_files $uri /index.html;
    }
}
